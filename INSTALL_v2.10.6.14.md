# Install Promote to King v2.10.6.14

Install over the canonical v2.10.6.13 deployment using the supplied incremental package and updater.

After installation, hard-refresh `ui-v2.html` once so the browser retrieves the v2.10.6.14 dashboard/Admin assets.

Expected visible Admin correction: card details grow to their content, redundant legacy navigation is removed in embedded mode, and activity-aware analyzers operate normally.
