name: P2K v2.13.1 package

on:
  push:
    branches: [release/v2.13.1]

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - name: Build exact incremental installer
        run: |
          bash tools/release/v2131/build-incremental.sh "$RUNNER_TEMP/v2131"
          unzip -t "$RUNNER_TEMP/v2131/PromoteToKing_v2.13.1_INCREMENTAL_FROM_2.13.0.zip"
          sha256sum "$RUNNER_TEMP/v2131/PromoteToKing_v2.13.1_INCREMENTAL_FROM_2.13.0.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: PromoteToKing-v2.13.1-incremental-${{ github.sha }}
          path: ${{ runner.temp }}/v2131/PromoteToKing_v2.13.1_INCREMENTAL_FROM_2.13.0.zip
          if-no-files-found: error
          retention-days: 30
