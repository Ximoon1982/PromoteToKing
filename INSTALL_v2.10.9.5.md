# Install Promote to King v2.10.9.5

Use the v2.10.9.4 → v2.10.9.5 incremental installer from the deployed P2K root.

The release preserves runtime data and does not modify the database schema or crontab. Replaying the same v2.10.9.5 installer is supported.
